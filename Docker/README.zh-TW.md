# Docker

## 課程簡介

90 分鐘的 Docker 實戰課程。從 container 基本概念出發，學會啟動與檢查 container，用 Docker Compose 編排多個 container，最後動手寫 Dockerfile build 出自己的 image 並 push 到 Docker Hub！

## 適用對象

- 用過 command line 的開發者
- 沒碰過容器技術、不需要 Docker 先備知識
